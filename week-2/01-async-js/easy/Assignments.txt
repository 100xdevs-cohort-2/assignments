function calculateTime(n) {
  let start = Date.now();
  var sum = 0;
  for (var i = 1; i < n; i++) {
    sum += i;
  }
  console.log(sum);
  return Date.now() - start;
}

console.log(calculateTime(10000) + " ms");